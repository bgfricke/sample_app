<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
	<% # Rails automatically loops over list of users %>
	<% # Rails automatically finds partial _user.html.erb %>
	<%= render @users %>
</ul>

<%= will_paginate %>